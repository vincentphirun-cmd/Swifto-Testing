Swifto circular avatar — shows a photo when `src` is supplied, otherwise the user's initials on a primary/10 disc (or a person glyph when nameless).

```jsx
<Avatar name="Maia Tipene" />
<Avatar name="Sam Lee" src="/uploads/sam.jpg" size="lg" />
```

Sizes: xs 28px, sm 36px, md 48px, lg 64px.
