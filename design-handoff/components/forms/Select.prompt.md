Swifto dropdown — native `<select>` styled like Input with a brand chevron. Used for job category and browse filters.

```jsx
<Select
  label="Category of Job"
  placeholder="Select a category"
  options={[{ value: 'moving', label: 'Moving' }, { value: 'cleaning', label: 'Cleaning' }]}
/>
```
