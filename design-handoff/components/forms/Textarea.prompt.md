Swifto multi-line input — same rounded chrome and focus ring as Input, used for "Experience" and "Availability" in job applications.

```jsx
<Textarea label="Experience" rows={4} placeholder="Tell us about your relevant experience…" />
```

Non-resizing by default. Supports the same `helper` / `error` props as Input.
